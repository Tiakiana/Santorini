using System;
using System.Collections.Generic;
using System.Text;

namespace Santorini.TICTACTOE
{
    class TicTacToeMiniMax
    {
        public void TakeTurn(TicTacToeBoard t)
        {
            
        }
        //public float EvaluateBoard(int[,] board) { 
        
        
        //}
        //public float MiniMax(int[,] board,int depth,bool ismaximizing)
        //{
        //    if (TicTacToeBoard.CheckForWinner(board)!= 0)
        //    {
        //        return TicTacToeBoard.CheckForWinner(board);
        //    }
        //    if (ismaximizing)
        //    {
        //        float bestval = -99999;

        //        foreach (var item in TicTacToeBoard.GetAvailableMoves(board))
        //        {
        //            int[,] newBoard = new int[3, 3];
        //            for (int x = 0; x < 3; x++)
        //            {
        //                for (int y = 0; y < 3; y++)
        //                {
        //                    newBoard[x, y] = board[x, y];
        //                }
        //            }
        //            newBoard[item.X, item.Y] = 1;
        //            float value = MiniMax(newBoard, depth + 1, false);
        //            if (value == 1)
        //            {

        //            }
        //        }
        //    }

        //}


    }
}
